[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — the app is a single-page client-side router (view state),
# so every path must serve index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
